## 무엇을 · 왜

<!-- 한두 문단. 무엇을 바꿨고 왜 필요했는지. diff를 옮겨 적지 않는다. -->

Closes #

<!-- 이 PR이 끝내는 이슈를 적는다. main에 병합되는 순간 GitHub이 그 이슈를 닫는다.
     이슈가 이 PR로 끝나지 않으면 `Closes` 대신 `Refs #`로 바꿔 쓴다 — 그러면 닫히지 않는다.
     base가 release/** 이면 병합 시점에 닫히지 않고, main으로 역머지될 때 닫힌다.
     닿는 이슈가 없으면 이 줄을 지운다. (`wiki/git.md` 2·3절) -->

## 검증

`apps/ch-life`에서 실행한 결과를 적는다.

- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm test:ci`
- [ ] 화면이 바뀌었다면 실제 기기·시뮬레이터에서 확인 (작은 화면 / 900px 이상 둘 다)

## 위키

동작을 바꿨다면 관련 `POL`/`RULE`/`CONTRACT`를 같은 PR에서 고쳤는지 확인한다 (`wiki/workflow.md` 5단계).

- [ ] `node wiki/check.mjs` 통과
- [ ] 해당 없음 (동작이 바뀌지 않았다)

## 리뷰어가 볼 곳

<!-- 없으면 지운다. 판단이 갈릴 수 있는 지점, 일부러 안 한 것. -->
