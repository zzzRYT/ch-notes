# Worktrees
.worktrees/

# Workspace dependencies & build output
node_modules/
dist/
*.tsbuildinfo

# 네이티브 빌드 산출물 — EAS에서 내려받은 바이너리는 수십 MB라 저장소에 담지 않는다.
# 배포본은 EAS/스토어가 정본이고, 로컬 파일은 언제든 다시 받을 수 있다.
*.ipa
*.apk
*.aab
*.app.tar.gz
*.dSYM.zip
