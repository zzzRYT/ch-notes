# Design: ch-life V1 — 한국어 설교노트 앱 (성경 자동삽입 + 가벼운 브라우저)

Generated by /office-hours on 2026-05-10
Branch: unknown
Repo: ch-life (local: /Users/leejaejin/coding/toy-project/ch-life)
Status: APPROVED
Mode: Builder

## Problem Statement

설교를 들으면서 노트를 적을 때, 성경 구절(예: "골 3:20")을 손으로 그대로 풀어 쓰거나, 별도 성경 앱·종이 주보·메모 앱을 오가며 베껴 적는 흐름이 비효율적이고 설교의 호흡을 끊는다. iOS·Android·태블릿에서 모두 같은 노트에 이어서 쓸 수 있고, 네트워크가 없어도 동작하며, 본문 자동삽입으로 "구절 적기"를 한 번의 키 입력으로 끝내는 도구가 필요하다.

본 프로젝트는 학습·재미 목적의 사이드 프로젝트이지만, "한국 성도가 설교 시간에 폰·아이패드·노트북으로 켜자마자 쓸 수 있는 단독 앱"이라는 빈 자리를 정확히 겨냥한다.

## What Makes This Cool

이 앱이 만들길 잘했다고 느껴지는 두 순간:

1. **설교 중 속도감.** 단축 키 하나로 "골 3:20" → 인용 블록에 본문이 풀린다. 다른 앱으로 이탈하지 않고, 설교의 흐름을 놓치지 않는다.
2. **설교 후 정리된 노트.** 설교가 끝난 뒤 다시 열었을 때, 내가 적은 머리글과 메모 사이에 성경 구절이 깔끔하게 박혀있다. 다시 읽고 보관·공유하고 싶은 결과물이 남는다.

이 두 순간이 모두 살아나는 게 이 앱의 존재 이유다.

## Constraints

- **기술 학습/재미 목적의 사이드 프로젝트.** 풀타임 회사가 아님. 한 사람이 주말·저녁에 만든다.
- **출시 목표는 3-5주.** 사이드 프로젝트가 6주 넘어가면 끝나지 않는다는 경험칙.
- **iOS + Android + 태블릿 반응형.** 웹은 V1 범위 밖.
- **오프라인-퍼스트.** 네트워크 없는 예배실·지하·산속 다 동작.
- **저작권 안전.** 공공도메인 텍스트만 사용. 추후 오픈소스 가능성 유지.
- **로컬 저장 (V1).** 서버·계정 없음. 디바이스에만 저장.
- **어른 친화 UX.** 한국 교회 어른들이 쓰는 앱이므로 큰 글씨·명확한 동선 우선.

## Premises

이 설계의 기반이 되는 합의된 전제 4개:

**P1.** 진짜 빈 자리는 "한국어 설교노트 앱"이라는 빈 카테고리가 아니라, "Obsidian/Spirit Notes에 익숙하지 않은 한국 성도가, 설치 직후 설교 시간에 켜서 바로 쓸 수 있는 단독 앱"이라는 배달 통로의 빈 자리다. ref → 본문 자동완성 기능 자체는 Spirit Notes 등에 이미 존재한다. 차별점은 발명이 아니라 "한국어 + 어른친화 + 단독 앱"이라는 묶음이다.

**P2.** V1 = **Expo (React Native) iOS + Android 앱.** 태블릿은 같은 코드의 반응형 레이아웃. 외장키보드(아이패드 + 매직키보드, 갤탭 + 키보드 케이스) 시나리오를 1급 시민으로. 오프라인-퍼스트 (Expo SQLite + 정적 자산 번들). 웹은 V1 범위 밖. 데스크탑·웹은 V2 의제.

**P3.** 본문 데이터는 직접 만들지 않는다. **개역한글 1961**(2012년 저작권 만료, 공공도메인) JSON을 `bible.mearie.org` 또는 `getbible.net`에서 가져와 정적 자산으로 번들(~3-4MB raw, ~1MB gzipped). 첫 실행 시 SQLite 또는 MMKV로 옮긴 뒤 영구 오프라인 사용. ref 파서는 `openbibleinfo/Bible-Passage-Reference-Parser`(MIT, TypeScript, 175KB/s)를 그대로 쓰고, **한국어 책 이름 매핑 어댑터**(예: "골" / "골로새서" / "Colossians" → 키 `Col`)만 직접 작성.

**P4.** 저장은 **로컬-온리** + **수동 JSON export**. "기기마다 따로 저장됨"은 V1의 의도된 한계가 아니라 V2의 명시적 의제. V1 안전장치로 "내 노트 전체 → JSON 파일 내보내기/가져오기" 기능 추가. "폰 잃어버려서 3년치 설교노트 날렸다" 케이스를 막는다.

## Approaches Considered

### Approach A: Minimal Sermon Note (S, 2주)

화면 2개(노트 목록, 에디터). ref 자동완성만. 성경 브라우저 없음.

- Pros: 가장 빠른 출시. Q1의 "속도감"에만 집중.
- Cons: Spirit Notes 한국어 카피로 보일 위험. V2 통합 비전(찬송·묵상)으로 자라기 어려운 정보구조.

### Approach B: Notes + Lightweight Bible Browser (M, 3-5주) ← 채택

A의 모든 것 + 사이드바(태블릿)/하단 탭(폰)으로 성경 브라우저(책→장→절). 절을 길게 누르면 "현재 노트에 인용 추가" 액션. 노트↔성경 양방향.

- Pros: Q1의 두 순간 모두 잡음 (속도감 + 정리된 결과물). 키보드 단축키(⌘B 브라우저, ⌘K ref 검색) 자연스럽게 들어감. V2(찬송·묵상·일정) 통합으로 자랄 정보구조.
- Cons: A보다 3주 더. 브라우저 UX 디테일(장 네비, 글꼴 크기, 다크모드) 들어감.

### Approach C: Audio + Notes (L, 6-8주)

A의 모든 것 + 설교 녹음 + 타임스탬프 노트. 노트 탭 시 그 시점부터 재생.

- Pros: Sermon Keeper 검증된 매력. 한국 교회 "녹음 켰어요" 익숙한 행위.
- Cons: 녹음 권한·iOS 백그라운드 정책·저장공간(1시간 ~30MB)·한국어 음성인식 정확도. 사이드 프로젝트 첫 출시에 얹으면 6주 후도 안 끝남.

## Recommended Approach

**Approach B — Notes + Lightweight Bible Browser.**

근거:

- A는 출시는 빠르지만 "Spirit Notes 한국어 포팅"에서 끝나기 쉽다. B는 정보구조에 노트↔성경 양방향이 들어가서, 메모리 속 통합 비전(설교노트 + 성경 + 찬송 + 묵상 + 일정)으로 자라는 골격이 된다.
- C는 매력적이고 시장 검증되어 있지만 V1 범위에 녹음을 얹으면 사이드 프로젝트 출시가 영원히 안 된다. C 요소들은 V2/V3로 명시적으로 미루고, B에서 "녹음 추가" 슬롯만 비워둔다(에디터 상단 빈 액션 버튼 자리).
- B가 Q1의 두 순간을 모두 잡고, "3-5주"는 사이드 프로젝트에 합리적인 첫 출시 창이다.

### V1 화면 구성 (4개)

1. **노트 목록** — 날짜순 카드. 검색(제목·본문·인용된 ref). 새 노트 버튼.
2. **노트 에디터** — 리치 에디터 + ref 자동완성. 인용 블록은 회색 좌측 바. 상단 액션: 저장(자동), 공유(텍스트 export). (V2의 녹음 버튼은 V2에서 추가 — V1 UI에 빈 슬롯을 미리 두지 않는다.)
3. **성경 브라우저** — 사이드바(태블릿 가로) / 하단 탭(폰). 책 → 장 → 절 3단 네비. 절 길게 누르면 액션 시트(아래 "절→노트 인용 흐름" 참조).
4. **절 보기** — 한 장 전체 본문. 다음/이전 장 버튼·단축키. "이 절 인용 추가" 버튼.

### 절→노트 인용 흐름 (cross-screen state)

브라우저에서 절을 길게 눌렀을 때:

- **현재 열려 있는 노트가 있으면 (탭/스택 어딘가에)** → 즉시 그 노트 끝에 인용 블록 추가, 토스트 "골 3:20 추가됨".
- **열려 있는 노트가 없으면** → 액션 시트 표시: ① 가장 최근 노트에 추가, ② 새 노트 만들기(제목 = 오늘 날짜), ③ 취소.

전역 상태(Zustand `currentNoteId`)로 추적. 노트 에디터 진입 시 set, 노트 목록으로 나가도 일정 시간(또는 명시적 "닫기" 전까지) 유지.

### 자동완성 UX 디테일

> **구현 갱신(2026-05-31):** 초기엔 리치 에디터 라이브러리(`@10play/tentap-editor`, WebView 기반)를
> 검토했으나, 노트마다 WebView+ProseMirror를 띄우는 무게와 어르신·구형 안드로이드 타깃을 고려해
> **채택하지 않았다.** 실제 구현은 **네이티브 RN 블록 에디터**다: 본문을 블록 배열(`BlockNode[]`)로
> 두고, 문단은 `TextInput`(`ParagraphInput`), 성경 인용은 별도 카드(`QuoteBlock`)로 렌더한다.
> 인라인 ghost text 데코레이션은 쓰지 않는다 — 대신 ref 패턴 뒤 **space**를 누르면 문단을
> head/인용/tail로 쪼개 인용 블록을 즉시 삽입한다.

- **트리거:** 단어 경계(줄 시작, 공백, 문장부호 뒤) + `[책 토큰][선택적 공백][숫자]:[숫자]` 패턴. 예: `골3:20`, `골 3:20`, `요한복음 3:16`. **단어 경계 안일 때만** — `그는골3:20말했다`는 트리거 안 함(중간 패턴 무시). 동일 줄 내 여러 ref 허용 (`골3:20 그리고 엡5:21`).
- **자동완성 표시:** 입력 중 화면 하단에 감지된 ref를 보여주는 **떠 있는 힌트 칩**(ref + `space` 안내). (당초의 인라인 회색 미리보기 위젯 대신.)
- **확정:** ref 직후 **space** → ref + 본문이 인용 블록으로 변환(문단이 head/인용/tail로 분할).
- **거부:** 패턴이 유효하지 않으면 힌트 칩이 사라짐. (유효 구절일 때만 칩 표시.)
- **데드 ref (예: `골 99:99`):** 룩업 실패 시 트리거 자체가 일어나지 않음(힌트 칩도 안 뜸).
- **책명 매핑:** 한국어 정식·축약·영어 (골/골로/골로새서/Col/Colossians). **데이터 출처 단일화: openbibleinfo `Bible-Passage-Reference-Parser-Languages`의 한국어 데이터를 1차로 가져오고, 누락분만 직접 보완.** P3와 일치.

### 키보드 단축키 (1급 시민)

수정자 키는 OS별로 매핑: **iOS/iPadOS = ⌘ (Command), Android = Ctrl**. UI 표기도 OS별로 자동 표시.

- `⌘/Ctrl + N` 새 노트
- `⌘/Ctrl + S` 저장 시각적 피드백 (실제 저장은 자동)
- `⌘/Ctrl + B` 성경 브라우저 토글
- `⌘/Ctrl + K` ref 빠른 검색 (모달)
- `⌘/Ctrl + F` 노트 내 검색
- `Tab` 자동완성 확정 / `Esc` 자동완성·모달 닫기

태블릿 + 외장키보드 시나리오(아이패드 매직키보드, 갤탭 키보드 케이스)를 1급으로 본다. **기술 노트:** Expo managed workflow에서는 `expo-keyboard` 또는 `react-native-key-command` 라이브러리로 가능. Week 0 PoC에서 Expo SDK 51+ 호환성 확인.

### 데이터 모델 (V1)

```ts
type Note = {
  id: string; // ulid
  title: string;
  body: BlockNode[]; // 블록 배열 (paragraph/heading/bullet/todo/blockquote/quote). DB엔 body_json TEXT로 저장
  createdAt: number;
  updatedAt: number;
  citedRefs: string[]; // ["Col 3:20", "John 3:16"] — 검색·인덱싱용
};

type BibleVerse = {
  book: string; // "Col"
  chapter: number;
  verse: number;
  text: string;
  translation: 'KRV';
};

type Settings = {
  // MMKV에 저장 (단일 row, key-value)
  fontScale: number; // 1.0 / 1.2 / 1.4 / 1.6
  themePreference: 'system' | 'light' | 'dark';
  lastOpenedNoteId?: string;
};

type ExportEnvelope = {
  // JSON export/import
  schemaVersion: 1;
  exportedAt: number;
  notes: Note[];
};
```

저장:

- **Expo SQLite** = `notes`, `verses`. 인덱스: `notes(updatedAt DESC)`, `notes(title)`, `verses(book, chapter, verse)` UNIQUE.
- **MMKV** = `settings` (단일 객체 직렬화).

**Import 정책:** ID 충돌 시 사용자에게 묻기(덮어쓰기/새 ID로 추가/건너뛰기). 스키마 버전이 다르면 import 거부 + 사용자 메시지.

**에디터 본문 포맷:** 사용자에게 "마크다운"이라는 단어를 노출하지 않는다. 내부 모델은 `BlockNode[]`이며 DB엔 `body_json` TEXT로 저장한다. 공유(내보내기/가져오기) 포맷은 **Markdown + YAML frontmatter**(gray-matter)로, 블록 ↔ 마크다운 직렬화/파서를 통해 변환한다. 인라인 강조는 텍스트 안에 경량 마크다운(`**굵게**`/`_기울임_`/`++밑줄++`)으로 저장.

### 기술 스택 (요약)

- **앱:** Expo SDK 51+ (RN 0.74+), Expo Router (file-based)
- **저장:** Expo SQLite (notes + verses) + `react-native-mmkv` (settings)
- **상태:** Zustand
- **에디터:** **자체 네이티브 RN 블록 에디터** (RN `TextInput` 기반, 블록별 렌더 + 성경 인용 카드). 리치 에디터 라이브러리(tentap/pell, WebView 기반)는 무게 때문에 미채택. WebView·ProseMirror 의존성 없음.
- **ref 파서:** `bible-passage-reference-parser` (npm, MIT) + openbibleinfo의 `Bible-Passage-Reference-Parser-Languages` 한국어 데이터.
- **성경 데이터:** **Week 0 라이선스 검증 통과한 소스에서** 개역한글 1961 JSON → 빌드 스크립트로 `assets/bible-krv.json`. 1순위: bible.mearie.org, 2순위: getbible.net Korean, 3순위: Wikisource KRV.
- **키보드:** `react-native-key-command` 또는 Expo SDK 내장 키보드 이벤트 (Week 0에 SDK 51+ 호환성 확인).
- **TS:** strict mode.
- **테스트:** Jest (파서 어댑터 단위 테스트 필수, KRV 66권 × 3형태 = 198 케이스), Maestro (E2E: "새 노트 → 골 3:20 입력 → Tab → 인용 블록 풀림" 골드 패스).

## Open Questions

V2 전에 결정할 필요는 없으나 V1 설계 시 마음에 둘 것:

1. **다른 번역본 추가 시점.** 개역개정·새번역은 저작권 이슈. KJV·ASV는 영어 공공도메인. V2까지는 KRV 하나로 살되, `BibleVerse.translation` 컬럼을 미리 두어 V2에서 추가만 하면 되도록.
2. **OS 텍스트 크기 vs 자체 글꼴 슬라이더.** OS 시스템 텍스트 크기를 따라가면 자체 슬라이더가 필요 없을 수 있음. Week 4에 OS 설정으로 충분한지 본인 테스트 후 결정.
3. **다크 모드 강제 옵션의 가치.** 예배실 조명이 어두워서 다크 모드가 사실상 필수일 가능성. 시스템 따라가기 + 설정에서 강제 dark 옵션만으로 충분.
4. **녹음 기능 (V2 후보).** Approach C에서 미룬 항목. V1 사용 후 본인이 "녹음이 있었으면 좋겠다"를 실제로 느끼는지를 V2 의제 진입 조건으로 둠.

## Success Criteria

V1이 "끝났다"고 부를 수 있는 기준 (실측 가능, 사이드 프로젝트 현실 범위):

- [ ] iOS/Android 빌드가 EAS Build로 나오고, 본인 디바이스에 설치되어 동작함
- [ ] 비행기 모드에서 새 노트 작성 → "골 3:20" → Tab → 본문 자동삽입 → 저장 → 재시작 → 노트 그대로 보임
- [ ] 아이패드 + 매직키보드에서 외장키보드 단축키 5개 이상 동작
- [ ] 책명 매핑이 KRV 66권 모두 (한국어 정식·축약·영어 3가지 형태)에서 동작 — Jest 198 케이스 통과
- [ ] **체감 지연 없음** — 노트 ~30개·자동완성 100회 직접 사용 시 눈에 띄는 지연 없음. (정량 SLA·프로파일링은 V1.1로 미룸. 이슈가 보일 때만 측정.)
- [ ] JSON export → 같은 디바이스에서 import (덮어쓰기/추가/건너뛰기 정책 동작) → 노트 보존됨
- [ ] **본인이 일요일 설교 1회 이상 실제로 사용**

## Distribution Plan

- **개발 빌드:** Expo Dev Client + EAS Build (free tier 충분)
- **테스트 배포:** TestFlight (iOS, $99/년 Apple 개발자 계정 필요), Internal Testing Track (Android, $25 1회)
- **프로덕션 배포:** App Store + Google Play (V1 범위 밖 — V1.1로 분리하여 스토어 등록·심사 마찰을 V1 출시와 격리)
- **OTA 업데이트:** Expo Updates — 심사 대기 없이 JS·자산 변경 즉시 배포 가능. 네이티브 변경만 빌드·심사.
- **CI/CD:** GitHub Actions에서 `eas build --platform all --auto-submit-no` (V1은 수동 업로드). V1.1에서 자동 제출.

**Week 0의 빌드 파이프라인 통과가 V1 출시 일정의 사전 조건.** 첫 빌드는 인증서·프로비저닝 문제로 실패하는 게 정상이며, 이 학습 비용을 V1 일정 안에 넣지 않는다.

V1 출시 = TestFlight + Internal Testing 본인 디바이스 + 1명 테스터 배포까지.

## Dependencies

블로커:

- Apple 개발자 계정 ($99/년) — TestFlight·App Store 둘 다 필요
- Google Play 개발자 계정 ($25 1회) — Internal Testing·Play Store 둘 다 필요

선행 학습:

- Expo SDK 51+ 기본 (Expo Router, Expo SQLite, EAS Build 5분 데모)
- TypeScript strict mode 익숙도
- 기본 RN 컴포넌트 (View/Text/ScrollView/TextInput) 익숙도

선행 결정:

- 에디터 라이브러리 1주 안에 PoC로 결정 (tentap vs pell vs 자체 구현) — **결정됨: 자체 네이티브 블록 에디터 (라이브러리·WebView 미사용).**

관련 작업 없음. 빈 디렉토리에서 시작.

## Next Steps (구체 빌드 순서)

V1은 **Week 0 (de-risk) + Week 1-5 (build)**. Week 0은 코드 짜기 전에 해야 할 검증.

**Week 0 — De-risk (코드 시작 전, ~1주 또는 주말 1일)**

이 세 항목 중 하나라도 막히면 P2/P3 재검토 트리거.

A. **에디터 PoC.** ~~`@10play/tentap-editor` 빈 앱에 붙이고, 자동완성 미리보기 위젯(decoration) 1개 부착 시도.~~ → **결론(구현 갱신): tentap/WebView 미채택. 자체 네이티브 블록 에디터로 구현** — 문단 `TextInput` + 성경 인용 카드, ref 뒤 space로 인용 블록 즉시 삽입.
B. **개역한글 JSON 라이선스 검증.** bible.mearie.org / getbible.net 둘 다 사이트 ToS·라이선스 명시 확인. 명시된 게 없으면 운영자 메일 또는 Wikisource KRV(공공도메인 명시)로 갈아탐. **결과를 디자인 doc에 한 줄 추가.**
C. **개발자 계정 등록.** Apple Developer($99/년) + Google Play($25 1회). Apple은 가입 후 인증 1-3일 걸릴 수 있음. EAS Build에서 빈 앱 → TestFlight·Internal Testing까지 한 번 통과시키기. 첫 빌드는 보통 인증서·프로비저닝 문제로 한 번 실패함 — 이 학습 시간을 V1 일정에서 격리.

**Week 1 — 토대**

1. Expo 프로젝트 생성 (TypeScript template), Expo Router로 화면 4개 라우팅 (빈 화면들)
2. 개역한글 JSON 가져오기 (Week 0 검증 통과 소스에서) → 빌드 스크립트로 `assets/bible-krv.json` 생성
3. SQLite 마이그레이션 스크립트, 첫 실행 시 verses 테이블 채우기 + Settings 객체 MMKV 초기화
4. Zustand 전역 상태 셋업 (`currentNoteId` 등)

**Week 2 — 자동완성 핵심** 5. ref 파서 어댑터 작성 + Jest 198 케이스 (KRV 66권 × 3형태). 통과 전 다음 단계 안 감. 6. 노트 에디터 화면 (Week 0 PoC 결과로 결정된 에디터) + 인용 블록 렌더링 7. 자동완성 미리보기 위젯 + Tab/Esc 키 핸들링 + 데드 ref 처리(빨간 "구절 없음") 8. **첫 통합 마일스톤: 빈 노트에 "골 3:20" → Tab → 인용 블록 풀림.** 이게 되면 V1 핵심이 살아난 것.

**Week 3 — 성경 브라우저 + 노트 목록** 9. 성경 브라우저 (책 → 장 → 절 3단). 사이드바(태블릿 가로) / 하단 탭(폰) 반응형 10. "절 길게 누르기 → 액션 시트 → 노트 선택/생성" cross-screen 흐름 11. 노트 목록 화면 + 검색 (제목/본문/`citedRefs`) 12. JSON export/import + 충돌 정책 UI

**Week 4 — 폴리시** 13. 키보드 단축키 5개 (OS별 ⌘/Ctrl 매핑) 14. 다크 테마 (시스템 따라가기 + Settings에서 강제 light/dark 옵션) + 글꼴 크기 슬라이더 (4단) 15. 본인 일요일 설교에 사용 + 부서진 것 고치기

**Week 5 — 배포 + 한 명 테스트** 16. Week 0에서 통과시킨 빌드 파이프라인으로 V1 빌드 → TestFlight + Internal Testing 업로드 17. 가족·교회 친구 1명에게 배포해서 5분 사용 관찰

## What I noticed about how you think

- **푸시백 패턴이 일관적이다.** P2 처음 합의할 때 "오프라인 가능성"을 들고 와서 RN을 다시 검토하자고 했고, 한 단계 더 가서 "웹은 버리자, 앱 전용"이라고 결정적으로 잘랐다. 두 번 모두 "어 그럼 그러죠"가 아니라 자기 사용 시나리오에서 출발한 반박이었다 — "보통 설교를 들으면서 패드와, 노트북을 활용하는 경우가 많으니깐". 이건 외부 권위(나, 검색 결과, 통념)를 자기 관찰보다 위에 두지 않는 사람의 사고 방식이다.

- **현실 제약을 일찍 묻는다.** 본문 자동삽입을 들으면서 처음으로 물은 것이 "이 부분도 오프라인에서 사용이 가능한 부분이야? 너무 양이 많으면 힘든거 아니야?"였다. 기능 명세보다 시스템의 한계 비용을 먼저 묻는다. 이건 만들기 전에 무엇을 만들 수 있는지 가늠하는 엔지니어링 사고다.

- **저작권을 미리 본다.** Q4에서 망설임 없이 공공도메인을 골랐다. "내가 쓸 거니까 괜찮겠지"로 도망가지 않고, "추후 공유 여지를 남기고 싶다"는 미래 옵션을 명시적으로 샀다. 사이드 프로젝트에서 이걸 하는 사람은 적다.

- **메모리에 적힌 통합 비전을 내려놓을 줄 안다.** ch-life는 메모리에 "설교노트 + 성경 + 찬송 + 묵상 + 일정"의 통합 앱으로 적혀있는데, V1을 설교노트 하나로 잘라내는 결정에 망설임이 없었다. 사이드 프로젝트 죽이는 1번 원인이 "다 잡으려다 다 못 잡음"인데, 이걸 본능적으로 안다.
